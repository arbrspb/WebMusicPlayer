# Runtime: Python 3.11, Windows
Flask==3.1.0
Werkzeug==3.1.3
Jinja2==3.1.6
waitress==3.0.2
requests==2.32.3
tqdm==4.67.1

# Audio and metadata
librosa==0.11.0
soundfile==0.13.1
mutagen==1.47.0
sounddevice==0.5.1
python-vlc==3.0.21203

# Optional local vocal-language detector (loaded only when enabled in UI)
faster-whisper==1.2.1

# Numeric and ML stack. NumPy 1.x is intentional for ONNX Runtime 1.17.
numpy==1.24.3
scipy==1.15.2
pandas==2.2.3
scikit-learn==1.6.1
numba==0.61.2
llvmlite==0.44.0
matplotlib==3.10.3
seaborn==0.13.2
psutil==7.0.0

# YAMNet + Discogs Multi-EffNet require exactly one ONNX Runtime variant.
# Install requirements-onnx-cpu.txt OR requirements-cuda.txt after this file;
# keeping ORT out of the common list prevents a later install from replacing
# a working GPU runtime with the CPU package.
comtypes==1.4.16
pycaw==20240210

# Local development and executable build
pytest==8.4.1
pyinstaller==6.13.0

# Linux deployment only; it is not used by the Windows launcher.
gunicorn==20.1.0; platform_system != "Windows"
